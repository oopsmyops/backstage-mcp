{
  "name": "backstage-mcp",
  "version": "1.0.0",
  "description": "Standalone MCP server for Backstage — catalog, scaffolder, and TechDocs tools for LLM clients",
  "license": "MIT",
  "type": "module",
  "main": "dist/backstage-mcp.mjs",
  "bin": {
    "backstage-mcp": "./dist/backstage-mcp.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/oopsmyops/backstage-mcp.git"
  },
  "keywords": [
    "backstage",
    "mcp",
    "model-context-protocol",
    "llm",
    "ai",
    "developer-portal",
    "catalog",
    "scaffolder",
    "techdocs"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && npm run bundle",
    "bundle": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/backstage-mcp.mjs --banner:js='#!/usr/bin/env node'",
    "start": "node dist/backstage-mcp.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0",
    "zod-to-json-schema": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
